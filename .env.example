# ── Backend ──────────────────────────────────────────────
PORT=3000

# ── WAHA (WhatsApp HTTP API) ─────────────────────────────
# Saat jalan via docker-compose, host WAHA = nama service "waha"
WAHA_URL=http://waha:3000
# Sesi khusus bot nominasi dan Re-Nominasi.
# Buat/start sesi ini di dashboard WAHA lalu scan QR dengan nomor bot.
WAHA_SESSION=nominasi-cilegon
# API key WAHA (kosongkan jika WAHA tidak pakai API key)
WAHA_API_KEY=

# ── Filter Trigger ───────────────────────────────────────
# ID grup WhatsApp target. Format WAHA biasanya: 1203630xxxxxx@g.us
# Kosongkan untuk MENERIMA dari semua chat (mode uji). Isi untuk produksi.
TARGET_GROUP_ID=
# Nomor yang diizinkan memicu reply + email di grup target (pisahkan dengan koma).
# Format boleh +62..., 08..., atau ID WAHA 62...@c.us.
ALLOWED_SENDER_NUMBERS=
# Kata kunci pemicu (pisahkan dengan koma). Pesan harus mengandung salah satu.
TRIGGER_KEYWORDS=Nominasi PGN,ReNominasi PGN

# ── Perilaku Balasan ─────────────────────────────────────
# true  = balas dengan ringkasan angka (Date | Swap | Stok | Total)
# false = balas singkat "Baik Pak, Data Re-Nominasi berhasil diinput"
REPLY_WITH_SUMMARY=true
# true = balas sebagai reply/quote ke pesan asli
REPLY_AS_QUOTE=true

# ── Email (Gmail / Microsoft 365) ────────────────────────
# Isi nilai asli di file .env (JANGAN di file contoh ini).
# Untuk Gmail gunakan App Password (bukan password akun biasa).
EMAIL_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
EMAIL_TO=
EMAIL_CC=
EMAIL_SUBJECT_PREFIX=Nominasi Harian PIP UBP Cilegon
